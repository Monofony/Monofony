#!/usr/bin/env bash

source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/../../../../bash/common.lib.sh"

print_header "Testing (PHPStan)" "AppName"
run_command "bin/phpstan analyse -c phpstan.neon -l 1 src"
