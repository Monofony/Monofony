app_backend:
    resource: "routes/backend.yaml"
    prefix: admin

app_frontend:
    resource: "routes/frontend.yaml"

app_api:
    resource: "routes/api.yaml"
    prefix: /api
    defaults:
        _format: json
