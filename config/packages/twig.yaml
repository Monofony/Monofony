twig:
    debug:            "%kernel.debug%"
    strict_variables: "%kernel.debug%"
    globals:
        app_name_meta:
            version: !php/const App\Kernel::VERSION
    default_path:  '%kernel.project_dir%/src/monofony-core/recipe/templates'
    paths:
        - '%kernel.project_dir%/src/monofony-core/recipe/templates'
        - '%kernel.project_dir%/src/monofony-backend/recipe/templates'
        - '%kernel.project_dir%/src/monofony-frontend/recipe/templates'
