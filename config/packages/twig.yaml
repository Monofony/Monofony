twig:
    debug:            "%kernel.debug%"
    strict_variables: "%kernel.debug%"
    globals:
        app_name_meta:
            version: !php/const App\Kernel::VERSION
    paths:
        - '%kernel.project_dir%/templates'
        - '%kernel.project_dir%/recipes/monofony-core/templates'
        - '%kernel.project_dir%/recipes/monofony-backend/templates'
        - '%kernel.project_dir%/recipes/monofony-frontend/templates'
